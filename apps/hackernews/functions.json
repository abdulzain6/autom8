[
  {
    "name": "HACKERNEWS__GET_STORIES",
    "description": "Fetches a list of stories from Hacker News based on a specified category (e.g., top, new, best).",
    "tags": ["hacker news", "stories", "top", "new", "best", "tech"],
    "visibility": "public",
    "active": true,
    "protocol": "connector",
    "protocol_data": {},
    "parameters": {
      "type": "object",
      "properties": {
        "story_type": {
          "type": "string",
          "description": "The category of stories to fetch. Defaults to 'top'.",
          "enum": ["top", "new", "best", "ask", "show", "job"]
        },
        "limit": {
          "type": "integer",
          "description": "The maximum number of stories to return. Defaults to 10."
        }
      },
      "required": [],
      "visible": ["story_type", "limit"],
      "additionalProperties": false
    }
  },
  {
    "name": "HACKERNEWS__GET_USER_DETAILS",
    "description": "Fetches public details for a specific Hacker News user, including their karma and creation date.",
    "tags": ["hacker news", "user", "profile", "karma"],
    "visibility": "public",
    "active": true,
    "protocol": "connector",
    "protocol_data": {},
    "parameters": {
      "type": "object",
      "properties": {
        "username": {
          "type": "string",
          "description": "The case-sensitive Hacker News username to look up (e.g., 'pg')."
        }
      },
      "required": ["username"],
      "visible": ["username"],
      "additionalProperties": false
    }
  },
  {
    "name": "HACKERNEWS__SEARCH_STORIES",
    "description": "Searches Hacker News stories using the Algolia Search API for a specific query.",
    "tags": ["hacker news", "search", "stories", "algolia"],
    "visibility": "public",
    "active": true,
    "protocol": "connector",
    "protocol_data": {},
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The keyword or phrase to search for (e.g., 'generative ai')."
        }
      },
      "required": ["query"],
      "visible": ["query"],
      "additionalProperties": false
    }
  }
]