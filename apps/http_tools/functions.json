[
  {
    "name": "HTTP_TOOLS__GET_URL",
    "description": "Fetches content from a URL using CycleTLS, extracts data using LLM instructions, and returns structured data according to the provided schema.",
    "tags": ["http", "get", "url", "fetch", "llm", "extraction", "structured", "schema"],
    "visibility": "public",
    "active": true,
    "protocol": "connector",
    "protocol_data": {},
    "parameters": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "format": "uri",
          "description": "The complete URL of the website to scrape. Must include protocol (http:// or https://). Example: 'https://example.com/page' or 'https://shop.com/product/123'"
        },
        "extraction_instructions": {
          "type": "string",
          "description": "Detailed LLM extraction instructions. Specify exactly what data to extract and how to structure it. Be specific about elements, formatting, and data types. Example: 'Extract product name, price in USD, availability status, and feature list from the product page' or 'Extract all trending repository names, authors, star counts, and descriptions from the GitHub trending page'"
        },
        "output_schema": {
          "type": "object",
          "properties": {},
          "required": [],
          "visible": [],
          "additionalProperties": true,
          "description": "Required JSON schema defining the exact output structure for validation. Must be a valid JSON Schema (Draft 7) with type definitions. Example: {\"type\": \"object\", \"properties\": {\"name\": {\"type\": \"string\"}, \"price\": {\"type\": \"number\"}, \"features\": {\"type\": \"array\", \"items\": {\"type\": \"string\"}}}, \"required\": [\"name\", \"price\"]}"
        }
      },
      "required": ["url", "extraction_instructions", "output_schema"],
      "visible": ["url", "extraction_instructions", "output_schema"],
      "additionalProperties": false
    }
  }
]